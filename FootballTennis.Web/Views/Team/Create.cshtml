@model CreateTeamViewModel

<form asp-action="Create" asp-route-tournamentId="@Model.TournamentId" method="post" data-player-select-form>
    @Html.AntiForgeryToken()

    <div class="row justify-content-center">
        <div class="col-12 col-md-6 col-lg-5">

            <input type="hidden" asp-for="TournamentId" />
            <input type="hidden" asp-for="TeamPlayersCount" />

            <div class="mb-3">
                <label asp-for="Name" class="form-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            @for (int i = 0; i < Model.TeamPlayersCount; i++)
            {
                <div class="mb-3">
                    <label class="form-label">Hráč @(i + 1)</label>
                    <select name="SelectedPlayersId" class="form-select player-select" required>
                        <option value="">-- Vyber hráče --</option>
                        @foreach (var player in Model.Players)
                        {
                            <option value="@player.Id"
                                    data-assigned="@(player.IsAlreadyAssigned ? "1" : "0")"
                                    disabled="@(player.IsAlreadyAssigned ? "disabled" : null)">
                                @player.FullName
                            </option>
                        }
                    </select>
                </div>
            }

            <div class="d-flex justify-content-between">
                <a class="btn btn-outline-primary" asp-action="Detail" asp-controller="Team" asp-route-id="@Model.TournamentId">
                    Zpět
                </a>

                <button class="btn btn-primary" type="submit">
                    Vytvořit
                </button>

            </div>
        </div>
     </div>
</form>